#
# 2026-09-18 (R3-M) — three of the entries just above RE-MEASURED, and the
# `expl` class ROOT-CAUSED but deliberately NOT fixed.
#
# Binary: shared-tree target/debug/zshrs built 2026-09-18 14:16 at ff7a8d861c.
# Restrict form matters: `--fn-derive-only NAME` alone still runs every
# hand-written probe; `--fn-only NAME --fn-derive-only NAME` is the one that
# narrows the sweep.  `COMPTAB_KEEP_SCRATCH=1` (comptab_parity.py:451) keeps
# the generated init + probe report for a post-mortem.
#
#   21055f3134 `_object_classes` / `_bpf_filters`, re-measured verbatim:
#     # 0 passed, 4 failed, 4 call(s)
#     cell/rowlist/col0 | ref/expl[#] = | test/expl[#] = ''
#   so the class is unchanged: zsh leaves the caller's `expl` with ZERO
#   elements, zshrs leaves ONE EMPTY element.
#
#   NOT the cause, tested and excluded: the probe dumps with
#   `v=( "${(@P)n}" )` (comptab_parity.py:5949), and a `(@P)` that yielded one
#   empty word for an empty array would produce this diff with no leak at all.
#   Both shells agree — empty array 0 words, two-element array 2, empty scalar
#   1, `${(tP)}` = array-local.
#
#   STRUCTURAL ROOT CAUSE (read, not inferred): a port reached through the
#   router is not a parameter scope.  `_object_classes`'s fpath file is NOT a
#   self-calling definition (`local expl` + `_wanted …`, no `name() { … }` /
#   `name "$@"`), so `run_port` (router.rs:302-305) takes its early-return
#   branch and never enters `doshfunc` — where C does
#   `startparamscope()` / `execode` / `endparamscope()`
#   (c:Src/exec.c:6254-6260).  `_wanted_impl` then pairs
#   `inc_locallevel()` (Base/Core/_wanted.rs:180) with `dec_locallevel()`
#   (:205) and NO `endparamscope`, and `dec_locallevel`
#   (src/ported/utils.rs:11287-11289) only decrements the counter — it never
#   runs C's `scanhashtable(paramtab, …, scanendscope, …)`
#   (c:Src/params.c:5862-5869), which is what deletes params stamped at a
#   level above the new `locallevel` (c:Src/params.c:54).  So a port's
#   `declare_locals` can never be unwound: nothing in the path it actually
#   takes ends a scope.
#
#   NOT FIXED, on purpose.  The obvious repair — one shared entry/exit guard
#   wrapping every port in `startparamscope`/`endparamscope` — is only
#   faithful if `setaparam` ASSIGNS to an existing outer parameter instead of
#   creating one stamped at the current `locallevel`.  `_description` must
#   write the array named by its `$2` into its CALLER's scope (upstream
#   Base/Core/_description sh:118, and the port says so at
#   _description.rs:255-256); under a blanket guard that write would be
#   unwound when the port returns, which is exactly why a whole-scope unwind
#   was rejected before.  Landing this needs the create-vs-assign semantics
#   settled first; a per-port patch would only move the symptom.
#
#   WHICH WRITE leaves the single empty element is still unattributed.  A
#   keep-scratch re-run is NOT a clean measurement here: it reported
#   `_alternative:71: command not found: __fasd_dirs_comp` on the reference
#   side, so that run's report cannot be read as a parity verdict.
#
#   7a077463ab `_mac_applications` — the "nmatches 1032 (zsh) vs 0 (zshrs)"
#   reading above is WITHDRAWN (R3-BC, same binary).  With identical stock
#   sources and `setopt extendedglob`, BOTH shells give
#   `rc=1 count=756 type=array-unique first=/Applications/AAS Player.app`;
#   `mdutil -s /` reports "Index is read-only", so both take
#   `_mac_apps_old_retrieve`.  The cells now TIMEOUT on the measurement
#   budget rather than FAIL.  An earlier `bad pattern` failure was a missing
#   `extendedglob` in the probe, not a defect.
#
#   7a077463ab `_as_if` — the "zsh lists 6900 files, zshrs 32" reading is
#   likewise NOT reproducible: 1 passed / 1 failed, and the failing cell
#   differs in row 0 only — `ref/$ cat -- $ZPF_OUT` vs `test/$ true`.
#
#   8eeb73f7bd `_zmodload` — reclassified as the FPATH-PRIORITY PRODUCT
#   DECISION, not a port bug.  The missing `_lastdescr` entry is `'files'`
#   (not `'file'`):
#     ref  /_lastdescr[#] = '' 'param' 'module file' 'files' 'file' 'module alias'
#     test /_lastdescr[#] = '' 'param' 'module file'         'file' 'module alias'
#   zsh's sh:72 `_files` is the USER's zpwr `_files`, whose
#   `_alternative 'files:files:…'` adds that descr; the port calls the Rust
#   `_files`, which never makes that second `_description` call.  Dispatching
#   `_files` by name reaches the zpwr function but diverges far worse (539
#   matches vs zsh's 1, R2-J), so it stays as it is.  Same root as the
#   bundle-appended-last fpath ordering recorded elsewhere in this file.
