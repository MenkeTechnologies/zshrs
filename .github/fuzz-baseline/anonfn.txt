# parity-fuzz --mode anonfn baseline: normalized signatures of KNOWN gaps.
# A divergence whose signature is here is allowed; a NEW one fails CI.
# Regenerate: parity-fuzz --mode anonfn --count N --verify 3  (copy NEW-signature lines).
