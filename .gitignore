/target
/compsys/target
zsh/
docs/PORT.md

# Paid products — companion book + reference manual PDF + their build pipeline —
# live in the private menke-books repo. Never track them here. The free docs
# landing page (docs/index.html), scripts/gen_grammar_docs.py (which feeds
# index.html), and the free HTML reference (docs/reference.html, linked from
# index.html) stay tracked and published. Only the PDFs, their LaTeX/Markdown
# sources, and the gen pipeline are private.
docs/book.md
docs/book.tex
docs/book.pdf
docs/reference.tex
docs/reference.pdf
docs/.fonts/
scripts/book_pdf.sh
scripts/reference_pdf.sh
scripts/pdf_theme_lib.sh
scripts/reference_pdf_theme.tex
scripts/update_reference_html.sh
# Runtime artifacts generated by `zshrs-recorder` / `zshrs` when the
# recorder corpus tests use a corpus-dir as $HOME — the daemon side
# writes its own toml + sqlite files into $HOME/.zshrs/.
tests/recorder_corpus/*/.zshrs/

# Raw `zstyle -L` capture of the daily-driver config, used as the SOURCE for
# scripts/parity_zstyle.zsh. Untracked on purpose: a raw dump carries the real
# `hosts` lists (internal addresses) and absolute home paths. Regenerate the
# committed, sanitized fixture from it with:
#     scripts/dump_live_zstyle.py scripts/parity_zstyle.zsh --from-file scripts/zstyle.zsh
scripts/zstyle.zsh
