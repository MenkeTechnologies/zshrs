typeset -g VAR=value
typeset -gA hash
typeset -ga arr
typeset -i num=42
typeset -F float=3.14
typeset -r readonly_var=fixed
typeset -x exported=yes
typeset -U unique_arr
declare -A H
local -i counter
export PATH=$PATH:/new/path
readonly LIMIT=100
integer count
float pi
