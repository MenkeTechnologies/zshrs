# Snapshot of free `fn` names in src/ported/**.rs that have no
# matching function definition in zsh's upstream C source. Each line
# is one identifier — the test reads this file and treats anything in
# it as "exempt-for-now". Anything NOT in this file that appears as a
# free fn in src/ported/ AND has no C counterpart fails the test.
#
# Process to fix a violation: inline the body at every call site (or
# rename to match a real C function name), then remove the line from
# this file. The list shrinks as the port catches up to the freeze.
#
# To regenerate after intentional removals:
#   cargo test --test ported_fn_names_match_c -- --nocapture 2>&1 \
#     | grep "no C counterpart" \
#     | sed -E 's/.*fn ([a-zA-Z_][a-zA-Z_0-9]*).*/\1/' \
#     | sort -u > /tmp/new_violations.txt
#   then merge with the existing list, keeping the header.
#
# Lines starting with `#` are comments. Blank lines ignored.

# Rust-only architectural helpers — no C counterpart by design.
# zsh C uses the global `sigtrapped[]` / `sigfuncs[]` arrays
# directly (no accessor function); zshrs needs a singleton to own
# the OnceLock<TrapHandler>. Inlining at 18 call sites would mean
# repeating `TRAPS.get_or_init(...)` everywhere.
# zsh C forks before installing handlers, so it has no analog. Rust's
# Rayon worker pool means the signal handler may run in a forked
# child and must skip touching parent state. Used by glob.rs's parallel
# expansion path to disable post-fork worker thread access.

# Color-distance / 256-palette mapping helpers — extracted from
# nearcolor module's main lookup logic. zsh C does this inline.

# Rust-only RNG primitives — zsh C uses `random()` libc directly.
# We wrap to centralise the seed/state and provide ChaCha20Rng for
# tests. These names exist nowhere in zsh C.

# OpenSSH BSD setresuid/setresgid shim helpers — these are Rust
# wrappers around libc::setres{uid,gid}() with errno save/restore;
# zsh C has the same shim file (openssh_bsd_setres_id.c) but uses
# direct errno macros, not function-shaped helpers.

# init.rs argv-parsing / help-text / exe-resolution helpers —
# zsh C inlines this work in `parseargs()` and `init_main()`.
# Each is single-caller and < 30 lines.

# tcp.rs Rust-side helpers extracted from `bin_ztcp` (Src/Modules/
# tcp.c). C zsh inlines connect/listen/accept/etc. inside one large
# bin_ztcp dispatcher; Rust split into modular helpers for testability.

# pattern.rs / glob.rs extracted helpers (parse / match-impl / range
# compare). zsh C inlines this work in `pattern.c::patcompile`,
# `pattern.c::patmatch`, and `glob.c`'s sort comparators.

# watch.rs Rust-side utmp/utmpx readers + format helpers — zsh C
# inlines this work in `watchlog2` (Src/Modules/watch.c).

# utils.rs Rust-side stdlib wrappers and zshrs-original helpers
# kept for ergonomics — zsh C inlines these or uses macros.

# math.rs — `name[idx]OP rhs` subscript-arith string parsers. zsh C
# does not pre-parse the assignment text: its `setmathvar()` (math.c:972)
# walks the `lval` left in place by the lexer/parser, so subscripts
# fall out of the regular lexer path. zshrs sees the raw `((a[i]+=expr))`
# text and must split it before evaluating; these three parsers do that
# split. Moving to src/extensions/ once the lexer can re-emit subscript
# tokens directly.
parse_compound
parse_pre_inc
parse_assign

# math.rs save_state/restore_state — Rust-side wrappers for C
# `mathevall()` (Src/math.c:367)'s xyy* stack-locals save/restore
# pattern. C inlines the saves directly in mathevall's body; the
# Rust port factors them into helpers since recursive eval happens
# from two callsites (callmathfunc arg parsing, getmathparam
# indirect-string eval) and inlining would duplicate ~17 lines twice.
save_state
restore_state

# module_features (rlimits.rs and other modules) — Rust-only OnceLock
# accessor for the C `static struct features module_features` global
# (rlimits.c:873). C inits the static at compile-time; Rust requires
# get-or-init since `features` contains fn-pointer fields that aren't
# const-initializable. Same name as the C static.
module_features

# index_to_name (options.rs) — reverse-direction lookup of zsh's
# `OPT_*` index → option name. C source iterates `optiontab` (HashTable
# keyed by name) and reads `Optname.optno` to map name→index; the
# reverse direction has no C counterpart since it's implicit in the
# order of OPT_* enum entries paired with the `optns[]` array. The
# Rust port collapses both into one match-based lookup.
index_to_name

# printf_format helpers (builtin.rs) — `printf` spec formatter
# fragments. C zsh inlines width/precision parsing in bin_print
# (Src/builtin.c:4791+); the Rust port factors it into helpers so
# each numeric vs string spec branch stays readable. Each helper
# corresponds to one `case '%[xs...]':` arm in bin_print and is
# only called from `printf_format`.
format_spec_str
format_spec_int
format_spec_uint
format_spec_float
parse_width_prec

# optno_by_name (options.rs) — forward direction of the same
# `optiontab` lookup C does at `Src/options.c:709-711` via
# `optiontab->getnode(optiontab, name)` returning `Optname.optno`.
# Until `optiontab` is stood up as a runtime hashtable, this helper
# walks `index_to_name(idx)` over `1..OPT_SIZE` and returns the
# first matching optno. Required so `optlookup` / `optlookupc`
# return canonical zh::OPT_* constants matching what
# `isset(optno)` / `opt_name(optno)` expect.
optno_by_name

# rlimits.rs — Rust-only helpers in the faithful port of
# Src/Builtins/rlimits.c. C uses preprocessor / direct globals where
# Rust needs runtime helpers:
#   nlimits — wraps `RLIM_NLIMITS as usize` to abstract the per-libc
#     return type (`c_int` macOS vs `__rlimit_resource_t` Linux).
#   ensure_limits_initialized — seeds `LIMITS`/`CURRENT_LIMITS` on
#     first use; C does this inline in `init_main()` (Src/init.c:1287).
#   leak_unknown_name — `Box::leak`s the `UNKNOWN-N` placeholder so
#     `resinfo_T.name` (`&'static str`) holds a stable pointer; C
#     uses `zalloc(12)` (rlimits.c:207) which is never freed for the
#     program's lifetime either.
#   lookup_resinfo — bounds-checked + lock-acquired access to RESINFO;
#     C inlines `resinfo[lim]->...` everywhere.
nlimits
ensure_limits_initialized
leak_unknown_name
lookup_resinfo

# sched.rs — Rust-only helpers in the faithful port of
# Src/Builtins/sched.c. After the 2026-05 line-by-line rewrite, the
# only Rust-only fns remaining are:
#   schedcmds_lock / schedcmdtimed_lock — OnceLock get-or-init for the
#     two module statics (`static struct schedcmd *schedcmds;` /
#     `static int schedcmdtimed;`). C dereferences directly; Rust
#     can't because OnceLock requires get-or-init.
#   checksched_thunk — `fn()` adapter for `addtimedfn` whose signature
#     is `fn(fn(), i64)`. C `checksched` returns void, Rust port
#     returns i32 so module entries can propagate status; thunk drops
#     the return.
schedcmds_lock
schedcmdtimed_lock
checksched_thunk

# curses.rs — Rust-only helpers in the faithful port of
# Src/Modules/curses.c. C dereferences libncurses globals directly
# (`stdscr`, `LINES`, `COLS`, internal window list); Rust port
# substitutes module-static OnceLock<Mutex<…>>'s + ANSI-escape
# emission since libncurses isn't linked.
#   windows_lock / initialized_lock / color_pairs_lock /
#     next_pair_lock — OnceLock get-or-init for the four module
#     statics (port of curses.c file-scope `zcurses_windows`,
#     `colorpairs`, `next_pair`).
#   terminal_size — TIOCGWINSZ probe replacing libncurses's `LINES`
#     / `COLS` populated by `initscr()` from terminfo.
#   cbreak — termios shim replacing libncurses's `cbreak()` call.
windows_lock
terminal_size
cbreak

# curses.rs additions for the validation-pipeline rewrite. C
# dereferences module-static ints/HashTables directly; Rust port
# wraps each in OnceLock<Mutex<…>> with these get-or-init helpers.
# zccmd_notavail and sgr_for_attrs substitute libncurses calls
# (`wattrset`, the curses-internal subcommand stubs) since the
# zshrs build doesn't link libncurses.
order_lock
errno_lock
colorpairs_lock
next_cp_lock
zc_errno_get
zc_errno_set
sgr_for_attrs

# curses.rs additions for the 11-zccmd_* finishing port:
#   flags_lock / mouse_mask_lock — OnceLock get-or-init for the
#     mouse-state module statics (zcurses_flags / zcurses_mouse_mask
#     in curses.c:99, 205).
#   read_key_sequence / csi_to_keypad / keypad_name —
#     termios+stdin read substituting for libncurses's
#     wget_wch/wgetch + the `keypad()` CSI translation table from
#     curses_keys.h (autogenerated from ncurses upstream).
#   colorpair_get_or_alloc — port of `zcurses_colorget()`
#     (curses.c:331); name diverges from C's `zcurses_colorget`
#     because the C signature returns Colorpairnode*, the Rust port
#     returns the i32 pair index directly.
flags_lock
mouse_mask_lock
read_key_sequence
csi_to_keypad
keypad_name
colorpair_get_or_alloc

# math.rs — MathState lifecycle / accessors. The struct mirrors the
# saved-and-restored locals C `mathevall()` (math.c:367) keeps across
# recursive entry; in C they are stack locals + module-statics so no
# fn ports them. `new` constructs the bag of locals; `with_*` are
# builder-style field setters; `extract_string_variables` and
# `getmathparams` read out the post-evaluation variables map for the
# caller (which zsh C does inline since the variables ARE the param
# table). `peek`/`advance`/`is_digit`/`is_ident_start`/`is_ident` mirror
# zsh C's `*ptr++` / `idigit()` / `iident()` macro idioms, but the C
# uses `unsigned char` arithmetic directly without an fn-shaped helper.
# `pop_with_lval`/`get_value`/`top_prec` extract per-token shapes that
# C inlines inside `pop()` / `mathparse()`.
new
with_variables
with_string_variables
with_c_precedences
with_force_float
with_octal_zeroes
with_lastval
extract_string_variables
getmathparams
peek
advance
is_digit
is_ident_start
is_ident
pop_with_lval
get_value
top_prec

# utils.rs — Meta-encoding helpers.
#   imeta_byte — Rust port of the `imeta()` macro from Src/zsh.h
#     (`#define imeta(c) ((c) >= Meta)`). Macros aren't in the
#     C-function index, so the name lands here.
imeta_byte
iwsep_byte

# utils.rs realpath — Rust wrapper around libc realpath(3). Not in zsh
# utils.c (zsh calls libc realpath directly). Kept for callsite shape.
realpath

# params.rs — Rust-only OnceLock<Mutex<…>> singleton accessors for the
# C globals referenced by the special-param GSU callbacks (ifs, home,
# term, etc.). C dereferences these globals directly; Rust port wraps
# each in a OnceLock with a get-or-init helper because Rust can't
# initialise statics with non-const values (env::var lookups, etc.)
# at compile time. One helper per C global, all lazily initialised on
# first param read. Same pattern as curses.rs windows_lock / utils.rs
# scriptname_lock.
ifs_lock
home_lock
term_lock
wordchars_lock
histchars_lock
keyboardhack_lock
histsiz_lock
savehistsiz_lock
zsh_terminfo_lock
zsh_terminfodirs_lock
cached_username_lock
pipestats_lock
shtimer_lock
pparams_lock
zunderscore_lock

# jobs.rs — Rust-only OnceLock<Mutex<…>> singleton accessors for the
# C globals `oldjobtab` / `oldmaxjob` (the snapshot used by `jobs -c`)
# and `origpgrp` (the parent shell's pgrp captured at startup, used by
# release_pgrp). Same pattern as the params.rs locks — Rust can't
# const-init non-trivial values. Plus capture_origpgrp() exposes a
# settable hook for shell startup; close_or_remove_filelist_entry is
# a pure helper for the fd-vs-name dispatch in deletefilelist that
# C inlines.

# hashtable.rs — Rust-only OnceLock<Mutex<ShFuncTable>> singleton
# accessor for the global C `shfunctab`. C zsh dereferences the global
# directly; Rust port wraps it behind get-or-init. simple_glob_match /
# glob_match_inner are inline helpers that C does via the full
# patmatch engine — split out as Rust helpers to avoid pulling in
# pattern.c for the simple `*` / `?` cases scanmatchshfunc needs.
shfunctab_lock
simple_glob_match
glob_match_inner

# hashtable.rs additional singleton accessors. C declares these as
# `mod_export HashTable cmdnamtab/aliastab/sufaliastab/reswdtab/histtab`
# globals; Rust port wraps each in OnceLock<Mutex<…>>. plus dircache_lock
# for the refcounted directory cache from C's static dircache array.
cmdnamtab_lock
aliastab_lock
sufaliastab_lock
reswdtab_lock
histtab_lock
dircache_lock

# text.rs — Rust-only OnceLock<Mutex<TextBuffer>> singleton + reset
# helper for the C file-static text-builder globals (tbuf, tindent,
# tpending, tnewlins, tjob, text_expand_tabs at Src/text.c:30+).
# C accesses these globals directly; Rust port wraps them in a
# struct held by a OnceLock, with text_buffer_reset replacing the
# inline init blocks at Src/text.c:298 / 333.

# signals.rs — Rust-only AtomicBool singleton + accessor pair for the
# C global `int interact` (Src/init.c). C dereferences the global
# directly; Rust port wraps in OnceLock-style AtomicBool. Same pattern
# as utils.rs scriptname_lock / errflag_lock.
interact_lock
set_interact
is_interact

# prompt.rs — Rust-only OnceLock<Mutex<TextAttrs>> singletons for the
# C file-statics txtcurrentattrs / txtpendingattrs (Src/prompt.c:1640+).
# C dereferences the globals directly; Rust port wraps each in a
# OnceLock plus a `set_pending_text_attrs` writer that callers use
# instead of the global assignment. Same pattern as utils.rs locks.
current_attrs_lock
pending_attrs_lock
set_pending_text_attrs

# prompt.rs — colour/zattr pieces hoisted from C macro bodies and
# inline branches in Src/prompt.c (set_colour_attribute ~2440,
# parsecolorchar, output_colour ~2136, match_colour ~336). zsh uses
# preprocessor macros and duplicated logic at each %F/%K site; Rust
# factors into private fns (no separate C symbol per helper).
color_rgb
color_get_rgb
color_to_ansi
color_from_name
zattr_set_fg_palette
zattr_set_fg_rgb
zattr_set_bg_palette
zattr_set_bg_rgb

# params.rs lookup_special_var — Rust-only GSU dispatch bridge. C zsh
# dispatches `$RANDOM` / `$USERNAME` / `$UID` etc. through
# `Param.gsu->getfn` inside getsparam (Src/params.c:3076). zshrs has
# the GSU callbacks ported (uidgetfn / randomgetfn / usernamegetfn /
# etc.) but no Param struct + paramtab callback table; this is the
# bridge that maps name → getfn so the shell's parameter-read path
# (fusevm_bridge::expand_param + subst.rs reads) can dispatch through
# the canonical GSU bodies. Logically equivalent to "Param.gsu->getfn"
# — same role, named for the lookup-by-name shape Rust callers use.
lookup_special_var

# params.rs initial_username — Rust helper used inside cached_username_lock's
# OnceLock init closure. Wraps libc getpwuid_r (C zsh's get_username() reads
# from getpwuid(getuid())->pw_name in init.rs). Broken out so the FFI-heavy
# init body doesn't bloat the closure literal.
initial_username

# params.rs set_zunderscore — Rust writer for `$_` (zunderscore_lock).
# C zsh updates `zunderscore` inline at the end of execcmd_exec
# (Src/exec.c); zshrs has no single execcmd_exec equivalent (commands
# dispatch through fusevm + multiple builtin handlers) so this helper
# is called from each command-completion site that needs to register
# the just-finished argv.
set_zunderscore

# modules/random.rs Rust-only helpers — were `RandomState::random_u32`
# / `random_u64` / `bounded` static methods on the dissolved
# RandomState bag-of-globals struct (Phase 2 dissolution). Now free
# fns; C inlines the same logic at random.c:79/104/etc. Multiple
# callers in this same Rust file (math_zrand_int, math_zrand_real,
# get_bound_random_buffer loop, Fisher-Yates shuffle) would each
# duplicate the 4-line `getrandom_buffer + decode` block.
random_u32
random_u64
bounded

# hashtable.rs HashNodeFlags trait — Rust-only abstraction over C's
# `struct hashnode { char *nam; int flags; ... }` header. Each typed
# table value impls this so the generic gethashnode/disablehashnode/etc.
# can filter+mutate without per-table dispatch. C uses raw ptr+offset
# into the embedded header.
HashNodeFlags
flags
set_disabled
is_disabled

# utils.rs zwarn/zerrnam family — module-static get/setters for the
# C globals scriptname / argzero / noerrs / locallevel /
# lineno (declared in Src/init.c, populated by the parser/source/fn
# entry paths). Rust port wraps each in OnceLock<Mutex<…>> with these
# helpers; C dereferences the globals directly. `errflag` itself is
# a `pub static AtomicI32` matching the C global name 1:1.
scriptname_lock
argzero_lock
noerrs_lock
locallevel_lock

# options.rs setemulate_opts_lock — Rust-only `get_or_init` helper for
# the C `static char *setemulate_opts;` file-static (options.c:501)
# that the per-option `setemulate` callback writes to and
# `installemulation` then splices into the caller's `new_opts`. C
# dereferences the global directly; Rust port wraps the
# OnceLock<Mutex<HashMap>>. Same pattern as utils.rs scriptname_lock.
setemulate_opts_lock
lineno_lock
shinstdin_lock
set_scriptname
scriptname_get
set_argzero
set_noerrs
set_locallevel
set_lineno
set_shinstdin
zwarning
argzero

# hashnameddir.rs — accessor for the C global `mod_export HashTable
# nameddirtab` (Src/hashnameddir.c:48). C dereferences the global via
# `nameddirtab->...`; Rust wraps a `Mutex<HashMap<String,nameddir>>`
# in `OnceLock` because `HashMap::new()` isn't const, then exposes
# the inner mutex through this accessor. Same pattern as utils.rs
# scriptname_lock / argzero_lock.
nameddirtab

# params.rs — accessors for the C globals `mod_export HashTable
# paramtab, realparamtab` (Src/params.c:515). C dereferences each
# via `paramtab->...`; Rust wraps `Mutex<HashMap<String, Param>>`
# in `OnceLock` and exposes the inner mutex via these accessors.
# Same pattern as nameddirtab / scriptname_lock.
#
# paramtab_hashed_storage — parallel storage for PM_HASHED param
# values. `param.u_hash` is typed `Option<HashTable>` per
# Src/zsh.h:1841 but the full HashTable runtime backing isn't
# wired yet; assoc-array values land here keyed on param name
# until that lands. C has no analog because C's u_hash holds
# the live HashTable.
paramtab
realparamtab
paramtab_hashed_storage

# params.rs sync_state_from_paramtab — port-transition shim that
# mirrors the global paramtab plus the parallel hashed-storage
# table into the three HashMaps `SubstState` carries during
# `prefork()` (Src/subst.c:100). C has no SubstState equivalent
# because the parameter table is the single source of truth; the
# helper exists only while subst.rs still reads from
# `state.variables`/`state.arrays`/`state.assoc_arrays`. Goes
# away once subst.rs reads through `paramtab().lock()` directly.
sync_state_from_paramtab

# pcre.rs — small Rust helpers wrapping pcre_exec-style usage for the
# zsh/pcre module port; zsh C inlines this in module entrypoints rather
# than naming these as separate C functions.

# watch.rs — Rust-only setters/predicates around the WTAB/LASTWATCH/
# LASTUTMPCHECK/WATCH thread_locals (port of Src/Modules/watch.c:150-156,
# 689 file-statics). C zsh has no setter functions: `$watch` assignments
# flow through the paramdef table (watch.c:697) and the elapsed-time gate
# is inlined inside `dowatch()` (watch.c:597). The Rust port factors them
# into named fns so future param-hook wiring has a single update site
# (and tests can drive them directly without going through the param
# table). `check_entry` extracts the per-entry filter inlined inside
# `watchlog()` (watch.c:458, lines 489-510).
set_watch_list
should_check
check_entry

# termcap.rs — `capability_lookup` is the Rust-only in-memory ANSI
# table replacing libtermcap's `tgetstr`/`tgetflag`/`tgetnum` triple.
# C source links against system libtermcap (or libtinfo) and reads
# `/etc/termcap` via tgetent(3); zshrs computes a minimal capability
# set inline based on $TERM so we don't drag libtermcap into the
# build (it's fragile across platforms — Linux uses libtinfo, macOS
# uses libncurses, BSDs use libtermcap proper). Function signatures
# + observable outputs of the public API (ztgetflag/gettermcap/
# bin_echotc/scantermcap) match C exactly; only this lookup helper
# is Rust-side.

# Port of zsh.h's `OPT_ISSET(ops, c)` / `OPT_MINUS(ops, c)` /
# `OPT_PLUS(ops, c)` accessor macros (zsh.h:1400-1414). C uses
# preprocessor macros that aren't function definitions — so they
# don't appear in zsh_c_fn_names.txt — but the Rust port needs them
# as inline fns so the per-builtin opt-spec parsers can call them
# uniformly. Each is a one-liner over the `[bool; 256]` bitmap that
# replaces C's `Options ops` per PORT_CHECKLIST.md rule 3.
# UPPERCASE names match C macro casing exactly (rule: macro casing
# preserved verbatim; #[allow(non_snake_case)] silences the lint).
OPT_ISSET
OPT_MINUS
OPT_PLUS
# Port of zsh.h's `PM_TYPE(X)` macro (zsh.h:1885) — same reasoning
# as the OPT_ family above. Const fn so callers can use it in const
# contexts like paramdef table initialisers.
PM_TYPE

# Port of ztype.h's `WC_ZISTYPE(X,Y)` / `WC_ISPRINT(X)` / `ZISPRINT(c)`
# multibyte-conditional macros (ztype.h:74-90). Same reasoning as the
# OPT_ family — preprocessor macros, not function definitions, so
# they don't appear in zsh_c_fn_names.txt. The Rust port wraps them
# as fns since `char` (Unicode-wide) needs the multibyte fallback
# path on every host. UPPERCASE names match C macro casing exactly.
WC_ZISTYPE
WC_ISPRINT
ZISPRINT

# Port of zsh.h's `QT_IS_SINGLE(x)` macro (zsh.h:294) and
# `MB_CHARLENCONV(str, len, cp)` macro (zsh.h:3287). Both are
# preprocessor macros — not C function definitions — so they don't
# appear in zsh_c_fn_names.txt. Same convention as the other
# zsh.h/ztype.h macro ports above.
QT_IS_SINGLE
MB_CHARLENCONV
# nameddir_table (hashnameddir.rs) — OnceLock accessor for the
# C `HashTable nameddirtab` global; HashMap::new() isn't const so
# the static must be lazily initialized through this fn. Wraps the
# Mutex used by all addnameddirnode/removenameddirnode/etc.
# shfunctab_table (builtin.rs) — OnceLock accessor for the
# C `HashTable shfunctab` global; HashMap::new() isn't const so
# the static must be lazily initialized through this fn. Wraps the
# Mutex used by add_autoload_function and other shfunctab call sites.
shfunctab_table
# traps_table (builtin.rs) — OnceLock accessor for the C `sigtrapped[]`
# / `siglists[]` arrays exposed via a name → body Mutex<HashMap>.
# HashMap::new() isn't const so the static is lazy-initialized.
traps_table
# ensure_termcap_loaded (modules/termcap.rs) — caches the result of
# tgetent(NULL, $TERM) for the lifetime of the process. The C source
# calls tgetent() inline at every consumer; the Rust port hoists this
# into a helper so each FFI call site stays a 1:1 port of its libtermcap
# call.
ensure_termcap_loaded

# printf_format (builtin.rs) — minimal printf-spec evaluator used by
# bin_print's -f/printf mode. The full C printf engine
# (Src/builtin.c:4691-5500) is much more elaborate
# (width/precision/flag chars/%b/%q/etc.); this Rust helper covers
# the common script subset until the full port lands.
printf_format

# sess_get / sess_with (modules/tcp.rs) — Rust-only field accessors
# for the thread_local ZTCP_SESSIONS Vec. The C source dereferences
# `Tcp_session sess` (a struct pointer) directly; Rust needs a
# RefCell::borrow / borrow_mut closure helper to get a safe view of
# the same fields without leaking borrow guards across statements.
sess_get
sess_with

# vared_zle_run (zle_main.rs) — Rust split of C bin_vared's c:1839-
# 1860 inner zleread call. The canonical bin_vared free-fn handles
# the flag-parse + variable-fetch (c:1678-1735), then delegates the
# actual ZLE edit to this helper so test callers can reach it.

# fdtable_lock / fdtable_get / fdtable_set (utils.rs) — Rust-only
# accessors for the `fdtable[]` global from Src/utils.c:67. The C
# source dereferences `fdtable[fd]` directly; Rust needs a Mutex<Vec>
# accessor pair so the table can grow lazily without a `growfdtable`
# port. addmodulefd in C just sets `fdtable[fd] = FDT_MODULE` inline.
fdtable_lock
fdtable_get
fdtable_set



# make_empty_special_pm (modules/parameter.rs) — Rust-only helper
# that pulls the common Param-construction shape used by
# getpmjob{dir,state,text} and getpmmodule into one place. C
# duplicates this 12-line construct inline at each callsite
# (c:1387/c:1459/c:1279/c:1042); Rust pulls it into one helper
# to avoid the repetition.
make_empty_special_pm


# opt_state_get / opt_state_set (options.rs) — Rust mirror of C
#  global from Src/options.c:36. C uses a flat
# char array indexed by optno; Rust uses HashMap<String,bool>
# because optno is FNV-hashed (no flat range).
opt_state_get
opt_state_set


# opt_state_get / opt_state_set (options.rs) — Rust mirror of C
# `opts[OPTSIZE]` global from Src/options.c:36. C uses a flat
# char array indexed by optno; Rust uses HashMap<String,bool>
# because optno is FNV-hashed (no flat range).
opt_state_get
opt_state_set


# default_on_options (options.rs) — Rust collapse of C's defset(on,
# emulation) macro from Src/options.c:73. The C macro reads the
# emulation flag bit off the per-option optns[] entry; placeholder
# until the optns[] flag table is ported.
default_on_options

# sigs_name / sigs_number (signals_h.rs) — accessor wrappers over
# the SIGS / ALT_SIGS pair-tables that port `sigs[]` and `alt_sigs[]`
# from signames.c (auto-generated) and jobs.c:2740. C indexes the
# `sigs[idx]` array directly (the awk-emitted layout is flat by
# signum); Rust uses a (name, signum) tuple table because libc
# signal numbers aren't dense or stable across platforms — the
# named accessor walks the table to provide the same lookup.
sigs_name
sigs_number

# getjob2 / findjobnam_c (jobs.rs) — Rust variants of the
# C getjob() / findjobnam() entry points. The `_c` / `_2` suffixed
# names accommodate Rust signature differences from the C entries
# while keeping the per-call data-flow shape; left in place since
# the canonical getjob/findjobnam already exist alongside.
getjob2

# lock_str / lock_vec (zle/compcore.rs) — Rust-only OnceLock
# get-or-init helpers for the completion-state global Mutexes (the
# COMPPREFIX / COMPSUFFIX / COMPLIST etc. ports of `char *` globals
# from complete.c:35-73). C dereferences the bare globals directly;
# Rust needs lazy initialization since `Mutex<String>::new()` is
# not const for OnceLock<Mutex<…>>. Each call site that previously
# would `&compprefix` now writes `lock_str(&COMPPREFIX).lock()`.
lock_str
lock_vec

# zsh.h macro ports (zsh.h:2559-2564) — C `#define` macros aren't
# functions so they don't appear in zsh_c_fn_names.txt. Rust ports
# them as inline fns for the same lookup semantics. `opt_name` is
# the reverse-lookup helper that maps optno constant → option name
# string for the HashMap-based OPTS_LIVE (C uses direct array index).
isset
unset
interact
jobbing
islogin
opt_name

# options.c macro + table ports (options.c:73, optns[] at c:79) —
# `defset` is a C `#define` macro, `optns_flags` extracts the flags
# field from the `optns[]` static table. Neither are C functions.
defset
optns_flags

# thingytab (zle/zle_thingy.rs) — Rust-only get-or-init accessor
# for the global `thingytab` HashTable from Src/Zle/zle_thingy.c:52.
# C declares the table as a top-level `mod_export HashTable thingytab`
# and references it directly across 11 functions (createthingytab,
# emptythingytab, refthingy, unrefthingy, rthingy, rthingy_nocreate,
# bindwidget, unbindwidget, addzlefunction, deletezlefunction,
# scanemptythingies). Rust uses OnceLock<Mutex<HashMap<String,Thingy>>>
# for thread-safe lazy init; this private helper hides the get_or_init
# boilerplate from every caller.
thingytab

# curkeymapname (zle/zle_keymap.rs) — Rust-only get-or-init accessor
# for the global `mod_export char *curkeymapname` from
# Src/Zle/zle_keymap.c:126. C declares it as a top-level char* and
# mutates via direct assignment from selectkeymap/bindkey -A. Rust uses
# OnceLock<Mutex<String>> for thread-safe lazy init; this private helper
# hides the get_or_init boilerplate from every caller.
curkeymapname

# keymapnamtab (zle/zle_keymap.rs) — Rust-only get-or-init accessor
# for the global `keymapnamtab` HashTable from Src/Zle/zle_keymap.c:128.
# Same pattern as thingytab — C declares it as `mod_export HashTable
# keymapnamtab` and references it directly across 8 fns
# (createkeymapnamtab, emptykeymapnamtab, freekeymapnamnode,
# linkkeymap, unlinkkeymap, openkeymap, refkeymap_by_name,
# unrefkeymap_by_name). Rust uses OnceLock<Mutex<HashMap<String,
# KeymapName>>>; this private helper hides the get_or_init.
keymapnamtab

# suffixlist (zle/zle_misc.rs) — Rust-only get-or-init accessor for
# the global `struct suffixset *suffixlist` from Src/Zle/zle_misc.c.
# C declares it as a top-level pointer; addsuffix prepends, fixsuffix
# walks+frees, iremovesuffix consumes. Rust uses OnceLock<Mutex<
# Vec<SuffixSet>>>; this private helper hides the get_or_init.
suffixlist

# isrch_spots (zle/zle_hist.rs) — Rust-only get-or-init accessor for
# the global `static struct isrch_spot *isrch_spots` + `int max_spot`
# from Src/Zle/zle_hist.c:946-947. C realloc-grows the array on
# demand; Rust uses OnceLock<Mutex<Vec<IsrchSpot>>>; this private
# helper hides the get_or_init from set_isrch_spot/get_isrch_spot/
# free_isrch_spots.
isrch_spots

# named_cmd_state (zle/zle_misc.rs) — Rust-only get-or-init accessor
# for the file-static globals `namedcmdstr`, `namedcmdll`, `namedcmdambig`
# from Src/Zle/zle_misc.c:1240-1244 referenced by scancompcmd and
# executenamedcommand. C declares them as top-level statics; Rust
# bundles them in NamedCmdState behind an OnceLock<Mutex<>> so the
# scan callback can mutate them while running; this private helper
# hides the get_or_init.

# ptycmds (modules/zpty.rs) — Rust-only get-or-init accessor for
# the file-static `Ptycmd ptycmds` linked-list head from
# Src/Modules/zpty.c:36, referenced from boot_/cleanup_/getptycmd
# /newptycmd/deletallptycmds/ptyhook. Rust uses OnceLock<Mutex<PtyCmds>>
# for thread-safe access; this private helper hides the get_or_init.
ptycmds

# hist.rs — Rust-only helpers for the C `histent` ring + per-entry
# inspection. C source uses raw pointer arithmetic on the
# `hist_ring` doubly-linked list and reads `histent` fields
# directly; Rust port wraps the ring as `Mutex<Vec<histent>>` and
# needs lookup helpers since `Vec::get` doesn't preserve the C
# `up`/`down` chain semantics.
#   make_histent — bag-of-fields constructor for the histent C
#     struct (Src/zsh.h `struct histent`); inlined at every C
#     call site as `zshcalloc(sizeof(struct histent))` + field-by-
#     field assignment (hist.c:1614/2098/etc.).
#   resolve_histfile — Reads `$HISTFILE` (C: getsparam("HISTFILE"))
#     since paramtab isn't a global yet in the Rust port.
#   ring_get / ring_position / ring_at / ring_len / ring_oldest /
#     ring_latest — `Vec::get` /index helpers replacing C's pointer
#     walks `for (he = hist_ring; he; he = he->down)` (hist.c:235+).
#   clone_histent — owned-clone of the struct since the C source
#     dups via `zalloc + memcpy` for the freehistent path.
make_histent
resolve_histfile
ring_get
clone_histent
ring_position
ring_at
ring_len
ring_oldest
ring_latest

# Pre-existing internal unit tests for params.rs special-param round-trips.
# These exercise the GSU callbacks/env-string/array-uniq helpers; no
# C analog because they are Rust-side test scaffolding.
test_libc_id_callbacks_match_libc
test_random_returns_15_bit_value
test_random_set_seeds_deterministically
test_ifs_round_trip
test_histsiz_clamps_to_1
test_savehistsiz_clamps_to_0
test_pipestat_round_trip
test_simple_arrayuniq_first_wins
test_split_env_string
test_mkenvstr
test_seconds_round_trip
test_argzero_round_trip
test_env_get_set
test_keyboardhack_one_char
test_histchars_default

# Internal helpers added by the params.rs port for scanparamvals.
# `pattry` is a per-port fallback for the pattern engine; the real
# function lives in Src/pattern.c and will replace this wrapper.
pattry

locallevel
inc_locallevel
dec_locallevel

# math.rs accessor for the C `lastbase` global (Src/math.c).
lastbase

# params.rs accessors for the C `static Param foundparam` global
# (Src/params.c:640). Rust-side singleton getters/setters around
# the OnceLock; no C analog because C accesses the global directly.
foundparam_lock
foundparam
set_foundparam

# Same pattern for the rest of the params.c:626-640 scanparamvals
# file-scope statics (numparamvals, scanprog, scanstr, paramvals)
# after the ScanParamValsState bag-of-globals dissolution per
# PORT.md Rule D.
scanprog_lock
scanstr_lock
paramvals_lock


# subst.rs typed wrappers around the global `errflag` AtomicI32
# (utils.rs). C accesses the global directly as `errflag` /
# `errflag |= ERRFLAG_ERROR`; we wrap to keep call-site idiom
# readable. No C counterpart by design.
errflag_set
errflag_set_error

# subst.rs paramtab read/write helpers — bridge to the canonical
# `ShellExecutor` parameter store via `fusevm_bridge::try_with_executor`.
# Replaced the deleted `SubstState.variables` / `state.arrays` /
# `state.assoc_arrays` field accesses (per user "SubstState must be
# removed" directive). C accesses paramtab directly via getsparam
# / getaparam; these are the executor-backed Rust equivalents that
# go through the executor (where the canonical typed paramtab lives).
# No C counterpart — architectural Rust-only seam to keep subst.rs
# free of `ShellExecutor` direct reaches per the
# no-shellexecutor-in-src/ported rule.
vars_get
vars_contains
vars_insert
arrays_get
arrays_contains
arrays_insert
assoc_get
assoc_contains
exec_assignaparam
exec_sethparam
exec_sync_state_from_paramtab
exec_getsparam

# subst.rs splice walker for zsh/parameter magic-assoc names
# (${commands[@]}/${functions[@]}/${aliases[@]}/etc.). C dispatches
# via per-table `scanpm<X>` walkers (Src/Modules/parameter.c:124+)
# each driven through a HashTable.scantab callback with a ScanFunc
# `fn(&HashNode, i32)` pointer that can't capture state. The Rust
# port collapses the per-name scanners into one match arm so the
# splice can `join` the values; the names dispatched here are the
# same set the per-key getpm<X> path handles via the const-fn arms.
splice_magic_assoc

# subst.rs SUB_FLAGS thread_local accessors. C stores `sub_flags`
# as a static int in subst.c (Src/subst.c:2169); Rust uses a
# thread_local Cell<i32> to keep callers re-entrant. The get/set
# pair is the singleton-accessor pattern, no C counterpart.
sub_flags_get
sub_flags_set

# glob.rs scaffold fns pulled out of `impl GlobData` to top-level
# per user directive. ALL Rust-only — pending a C-faithful rewrite
# of scanner() (glob.c:500) that uses lchdir-based descent +
# `insert()` (glob.c:346) inline qualifier eval + `parsepat`
# (glob.c:791) emitting a `struct complist`. Once that lands, these
# helpers are deleted (the AST + recursive-walk shape goes away
# in favor of C's wordcode-driven walk).
globdata_glob
parse_qualifiers
parse_qualifier_string
parse_uid_gid
parse_size_spec
parse_range_spec
parse_subscript
parse_pattern
scan_pattern
scan_recursive
check_qualifiers
check_qualifier_list
check_single_qualifier
sort_matches
apply_selection
# glob_path — canonical entry for filesystem glob expansion (mirrors
# zglob driver at glob.c:1214 with alternation + extendedglob
# pre-passes inlined). subst.rs `stringsubst` glob site routes
# through this. Will fold into a real `zglob` port when scanner()
# rewrite completes.
glob_path
# find_top_level_tilde — extendedglob `pat1~pat2` exclusion helper
# used by glob_path. C inlines the top-level-tilde scan in scanner()
# (glob.c:580+) when extendedglob is on.
find_top_level_tilde
# apply_modespec — mode-spec apply helper (qgetmodespec returns a
# ModeSpec at glob.c:844, this fn applies it). Rust-only because C
# inlines the bit manipulation at every callsite.
apply_modespec
# expand_glob_alternation — top-level `(a|b|c)` brace-style alternation
# pre-expansion that runs before the path-component scanner. C handles
# this via BRANCH opcodes in pattern.c's compiled Patprog; Rust port
# pre-expands at glob.rs level until the per-component Patprog port lands.
expand_glob_alternation

# Pre-existing zsh.h macro-shaped helpers (Src/zsh.h is a header,
# not in zsh_c_fn_names.txt which only lists fns from .c files).
IS_WRITE_FILE
WC_SELECT_SKIP

# C zfopendata inlines the PASV-response sscanf parse (Src/Modules/
# zftp.c:925-941); zshrs extracts it as a private helper used by
# both zfopendata and the higher-level zftp_session::connect() path.
parse_pasv_response

# C zftp writes `errno = ETIMEDOUT` / `errno = EIO` as plain assignments
# to the POSIX errno macro (Src/Modules/zftp.c:370, 1383, 1400, 1424).
# Rust libc exposes the thread-local accessor under different names per
# OS (`__error` macOS/BSD, `__errno_location` Linux/Android, `__errno`
# OpenBSD/NetBSD), so the cfg-gated pointer helper replaces the C macro.
errno_ptr

# C pattern.c uses native pointer arithmetic to insert opcodes into
# the bytecode buffer; the Rust port stores byte offsets explicitly,
# so when patinsert() shifts a chunk of bytecode, downstream next_off
# slots need fixup. C doesn't need an equivalent — pointers shift
# implicitly. Pure byte-offset-bookkeeping helper, no C analog.
fixup_offsets_after_insert
# Per-opcode payload-size dispatch table walker. C uses Upat pointer
# arithmetic and the `+1` / `+2` / `+sz` offsets inline in patmatch;
# Rust's byte-offset bytecode reader factors the size lookup into one
# place. Architectural helper, no C analog.
advance_past_instr

# Rust port renamed C's static `patmatch` interpreter (pattern.c:2694)
# to free the `patmatch` name for the public convenience entry. The
# interpreter signature differs from C's `int patmatch(Upat prog)`
# because Rust threads input/state through args rather than C's
# per-thread file-statics.
patmatch_internal

# Byte-offset bytecode compile helpers — C does these inline via Upat
# pointer arithmetic + macros; Rust factors them so byte-offset
# bookkeeping isn't duplicated at every callsite.
set_next
chain_branches_to

# hashtable.rs cmdnam constructor helpers. C builds cmdnam entries
# inline in `hashdir()` (hashtable.c:704/712) without a constructor
# fn — the inline literal sets `node.flags` and the appropriate
# union arm. Rust provides hashed/unhashed builders to keep the
# call sites readable; same two-arm dispatch as C.
cmdnam_hashed
cmdnam_unhashed

# Rust-only per-module helper bridging the canonical
# `module::featuresarray` / `handlefeatures` / `setfeatureenables`
# API (which takes `&Module`) to the per-module entry-point hooks
# (which receive `*const zsh_h::module` raw pointers from the
# module loader). Synthesizes an equivalent `module::Module` named
# after the static-link module. Same name + body in every
# clone/example/datetime/etc. file — pure dispatch glue, no logic.
module_handle

# Rust-only ShFunc constructor helpers — no single C function.
# C uses `createshfunc(name); shf->funcdef = parse_subst_string(body);`
# or `createshfunc(name); shf->node.flags = PM_UNDEFINED;` inline at
# the call site (hashtable.c:824, :829). Rust provides body/autoload
# builders to keep the call sites readable; same two-arm dispatch
# as C, plus the Rust-only `body: Option<String>` extension for the
# deferred-compile path documented on `struct shfunc` in zsh_h.rs.
shfunc_with_body
shfunc_autoload
getkeystring_with
apply_history_modifiers

# === lex.rs / parse.rs — absorbed from the standalone `zshrs-parse`
# workspace crate (2026-05-12). The crate's lexer + parser predate the
# strict-port discipline of `src/ported/`; many helpers below either
# wrap real zsh lexer state with idiomatic Rust safety guards
# (check_recursion / check_iterations are recursion-depth limiters; C
# uses raw stack overflow), do byte-level scratch work that lives
# inline inside `Src/lex.c::gettok` (read_line / lex_initial /
# lex_arith / process_heredocs / dquote_parse_inner are extracts of
# the C function body), or expose token-classification predicates
# that C inlines via `itok()` / `inblank()` / `iblank()` macros
# (is_token / is_blank / is_inblank). Long-term these all collapse
# into the canonical `gettok` body when that is fully ported.
check_recursion
check_iterations
is_blank
try_numeric_range_glob
is_inblank
process_heredocs
read_line
lex_arith
lex_initial
is_valid_assignment_target
dquote_parse_inner
untokenize_char
check_alias
inject_alias_text
take_raw_buf

# parse.rs Rust-only heredoc / inoutpar resolution — zsh's parser
# patches heredoc bodies directly into the wordcode buffer
# (`Src/parse.c::setheredoc` rewrites pc[1] / pc[2]). zshrs's AST
# parser threads a `HereDocInfo` list through and resolves redirs
# in a post-parse walk. simple_name_with_inoutpar reconstructs the
# `fna fnb fnc()` multi-name shorthand from the simple-command's
# wordlist — C parses this inline inside `par_funcdef`.
fill_heredoc_bodies
fill_in_sublist
fill_in_pipe
fill_in_command
resolve_redir
simple_name_with_inoutpar

# lex.rs `mod tests {}` test functions — build.rs's brace counter
# drifts on lex.rs (string literals containing `{` / `}` in the lexer
# scanner) so the `mod tests` exemption misses these. They are
# bona-fide `#[test]` fns under `#[cfg(test)] mod tests`. Long-term
# fix: make build.rs's collect_free_fns string-aware. Short-term:
# allowlist by name.
test_simple_command
test_pipeline
test_redirections
test_heredoc
test_single_quotes
test_function_tokens
test_double_quotes
test_command_substitution
test_env_assignment
test_array_assignment
test_process_substitution
test_arithmetic
test_semicolon_variants

# === P7-P8 ZshLexer dissolution: free fns at module scope ===
error
heredocs_clear
heredocs_clone
heredocs_is_empty
heredocs_len
heredocs_push
heredocs_set
heredocs_take
incasepat
incmdpos
incond
infor
input_slice
inredir
inrepeat
intypeset
isnewlin
lineno
pos
set_error
set_incasepat
set_incmdpos
set_incond
set_infor
set_inredir
set_inrepeat
set_intypeset
set_isnewlin
set_pos
set_tok
set_tokfd
set_toklineno
set_tokstr
tok
tokfd
toklineno
tokstr
tokstr_eq
tokstr_is_none
tokstr_is_some
tokstr_take
lex_init

# === P8 ZshParser dissolution: free fns at module scope ===
check_limit
parse
parse_case
parse_cmd
parse_for
parse_for_cstyle
parse_pipe
parse_program
parse_program_until
parse_redir
parse_select
parse_simple
parse_sublist

# === P8 followup ===
parse_init
par_event_wordcode
par_list_wordcode
par_sublist_wordcode
par_pipe_wordcode
par_cmd_wordcode
par_simple_wordcode
par_arith_wordcode
par_case_wordcode
par_cond_wordcode
par_cursh_wordcode
par_for_wordcode
par_funcdef_wordcode
par_if_wordcode
par_repeat_wordcode
par_select_wordcode
par_subsh_wordcode
par_time_wordcode
par_until_wordcode
par_while_wordcode
ecgetstr_wordcode
invoke_argv_wordcode
skip_form
exec_for_wordcode
exec_select_wordcode
exec_case_wordcode
exec_if_wordcode
exec_while_wordcode
exec_repeat_wordcode
exec_funcdef_wordcode
exec_subsh_wordcode
exec_cursh_wordcode
exec_timed_wordcode
exec_cond_wordcode
exec_arith_wordcode
exec_try_wordcode
exec_list_wordcode
exec_sublist_wordcode
exec_pline_wordcode
exec_cmd_wordcode
exec_simple_wordcode
exec_wordcode
